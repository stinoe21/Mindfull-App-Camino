# Shell scripts moeten LF houden, ook op Windows. Met CRLF weigert sh ze
# uit te voeren ("bad interpreter") en werkt de pre-push hook niet.
*.sh        text eol=lf
.githooks/* text eol=lf

# Lockfiles niet mergen, maar opnieuw genereren
package-lock.json -diff
