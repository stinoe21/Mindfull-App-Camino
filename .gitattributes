# De pre-push hook moet LF houden, ook op Windows. Met CRLF weigert sh hem
# uit te voeren ("bad interpreter").
.githooks/* text eol=lf

# Lockfiles niet mergen, maar opnieuw genereren
package-lock.json -diff
