The check-in input — a labeled slider between two opposite feelings, used for all four daily check-in dimensions (temperature, wind, visibility, changeability).

```jsx
<Slider leftLabel="Guur" rightLabel="Lekker zacht" value={38} />
```

Always framed by a lime thumb/track fill on a light neutral rail — never red/green, since the two ends aren't good/bad, just different. Pair with the matching mascot pose and a one-line non-judgmental prompt above ("Geen goed of fout...").
